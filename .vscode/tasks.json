{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start app",
            "type": "shell",
            "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File ${workspaceFolder}/scripts/start.ps1",
            "presentation": {
                "reveal": "silent"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}